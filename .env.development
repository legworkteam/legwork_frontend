# 로컬 개발용. 백엔드 없이 화면을 클릭해볼 수 있게 더미 응답을 씁니다.
# 백엔드가 뜨면 아래 줄을 false 로 바꾸고 VITE_API_PROXY 를 백엔드 주소로 맞추세요.
VITE_USE_MOCK=true

# VITE_USE_MOCK=false 일 때 dev 서버가 /api/v1 요청을 프록시할 주소 (CORS 회피)
VITE_API_PROXY=http://localhost:8000
